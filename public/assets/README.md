# Assets

[English](README_en.md)

存放网站运行时使用的静态资源。

- `images/`：页面图片和插图
- `icons/`：图标与 favicon
- `fonts/`：项目专用字体

只有实际加入资源后再创建对应子目录，文件名使用小写 kebab-case。

`model/` 保存 STEP 源模型、模型说明和 Web 解析流程，`showcase/` 保存主项目真实 Qt Quick 截图，`vendor/` 保存固定版本的 STEP/WASM 解析运行时及许可证。
